# The dev script sets WORKBENCH_DEV_RENDERER=1 automatically.
# Set this only for local renderer work. It is ignored by start and packaged builds.
# WORKBENCH_SKIP_AUTH=1

# Start the local FastAPI child process from Electron when its environment is ready.
# WORKBENCH_START_LOCAL_SERVICE=1
